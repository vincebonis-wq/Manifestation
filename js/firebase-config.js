/* =====================================================================
   CONFIG FIREBASE — active les COMPTES PARTAGÉS pour toute l'app.
   ---------------------------------------------------------------------
   Tant que ce fichier est vide, l'app tourne en mode LOCAL (sans compte).
   Dès que tu colles ta config ici, l'app affiche un écran de connexion :
   chaque personne (toi, un ami) crée SON compte, avec ses propres données,
   totalement isolées (voir firestore.rules). Toi seul es admin (console
   Firebase). Cette config n'est PAS secrète : elle est faite pour être
   publique dans une app web — la sécurité vient des règles Firestore.

   COMMENT LA REMPLIR :
   1. console.firebase.google.com -> ton projet -> Paramètres du projet
   2. Section "Vos applications" -> app Web -> objet firebaseConfig
   3. Recopie les valeurs ci-dessous, puis dé-commente le bloc.
   ===================================================================== */

// window.FIREBASE_CONFIG = {
//   apiKey: "…",
//   authDomain: "…",
//   projectId: "…",
//   storageBucket: "…",
//   messagingSenderId: "…",
//   appId: "…"
// };
